// import { browser } from '$app/environment';
// import { writable } from 'svelte/store';

// let local = window.localStorage;
// export const stored_object = writable(local.stored_object ? JSON.parse(local.stored_object) : {});

// stored_object.subscribe((val) => local.setItem('stored_object', JSON.stringify(val)));
